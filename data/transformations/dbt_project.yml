name: 'atlas_dbt'
version: '1.0.0'
config-version: 2
profile: 'atlas'

model-paths: ['models']
seed-paths: ['seeds']

models:
  atlas_dbt:
    staging:
      +materialized: view
    marts:
      +materialized: table
